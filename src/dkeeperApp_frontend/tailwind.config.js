/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ['./index.html', './src/**/*.{js,jsx,ts,tsx}'], // Include all relevant files
  theme: {
    extend: {}, // Extend or override default theme settings
  },
  plugins: [], // Add plugins here
};